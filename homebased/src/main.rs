//! homebased — the homebase daemon: the leased-KV kernel and the identity
//! resolver composed into one binary behind feature flags.
//!
//! This is a placeholder build reserving the crate name; the daemon lands
//! with the server milestones. See <https://github.com/nikhilgarg28/homebase>.

fn main() {
    eprintln!("homebased: placeholder build — the daemon lands with the server milestones.");
    eprintln!("see https://github.com/nikhilgarg28/homebase");
    std::process::exit(1);
}
